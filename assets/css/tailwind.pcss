@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    font-family: "Quicksand", sans-serif;
  }
}

@layer utilities {
  .main-gardient {
    @apply bg-gradient-to-r from-[#004876] to-[#1E71A6];
  }
}
